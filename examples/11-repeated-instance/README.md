# 11-repeated-instance

Run the TypeScript reference flow:

```bash
npx tsx --tsconfig tsconfig.json src/cad/run-assembly-tests.ts
```

This example's scenario is covered by the named cases in that runner
(see docs/ASSEMBLIES.md for the narrative walkthrough). Golden model:
80 x 50 x 12 = 48,000 mm^3 remains the single-part baseline.
